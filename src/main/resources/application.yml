server:
  port: 8081

spring:
  application:
    name: productos-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/productos_db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
